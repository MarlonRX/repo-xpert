// ── gadv lib ─────────────────────────────────────────────────────────
// Todo lo que NO es loop de terminal. Los tests de integración consumen
// el engine por acá (y F6 lo partirá a `crates/gadv-engine` si la API
// ya no cambia).

pub mod config;
pub mod engine;
pub mod log;
pub mod theme;
pub mod version;
